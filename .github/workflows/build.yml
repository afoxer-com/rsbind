name: Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --manifest-path toolchain/rsbind/Cargo.toml --verbose
    - name: Run tests
      run: cargo test --manifest-path toolchain/rsbind/Cargo.toml --verbose
